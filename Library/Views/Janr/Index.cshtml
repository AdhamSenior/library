@model IEnumerable<Library.Models.Janr>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.janr_nomi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.janr_nomi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JanrId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JanrId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JanrId })
        </td>
    </tr>
}

</table>
